apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-ingress-{{ namespace }}
  namespace: {{ namespace }}
  labels:
    app.kubernetes.io/name: nginx-ingress-{{ namespace }}
    app.kubernetes.io/part-of: nginx-ingress-{{ namespace }}
data:
  use-forwarded-headers: "true"
#  compute-full-forwarded-for: "true"
#  use-proxy-protocol: "true"
#  proxy-real-ip-cidr: "0.0.0.0/0"
#  hsts: "true"
