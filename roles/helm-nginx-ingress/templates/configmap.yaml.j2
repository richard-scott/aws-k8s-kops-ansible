apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-ingress-{{ k8s_namespace }}-controller
  namespace: {{ k8s_namespace }}
  labels:
    app.kubernetes.io/name: nginx-ingress-{{ k8s_namespace }}-controller
    app.kubernetes.io/part-of: nginx-ingress-{{ k8s_namespace }}-controller
data:
  ssl-redirect: "true"
  use-forwarded-headers: "true"
#  compute-full-forwarded-for: "true"
#  use-proxy-protocol: "true"
#  proxy-real-ip-cidr: "0.0.0.0/0"
#  hsts: "true"
