apiVersion: v1
kind: Service
metadata:
  name: hello-kubernetes
spec:
  ports:
    - name: http
      protocol: TCP 
      port: 80
      targetPort: 8080
  type: ClusterIP
  selector:
    app: hello-kubernetes
